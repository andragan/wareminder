## 1. Supabase Auth Configuration

- [ ] 1.1 Enable Google as a Supabase Auth provider and register the Chrome extension redirect URL
- [ ] 1.2 Configure the Chrome extension redirect URI generated by `chrome.identity.getRedirectURL(...)` and document the session-bearing redirect flow
- [ ] 1.3 Add environment/config support for the redirect URI and callback parsing behavior

## 2. Database Migration to Auth-Owned Identity

- [ ] 2.1 Create migration(s) to introduce a safe mapping from legacy `user_profiles` rows to `auth.users`
- [ ] 2.2 Backfill `auth.users` for existing customers and record the mapping needed for cutover
- [ ] 2.3 Backfill `subscriptions` and other user-scoped tables to the authenticated user key
- [ ] 2.4 Restore the final schema so user-scoped app data is keyed from or directly linked to `auth.users.id`
- [ ] 2.5 Drop `plan_type` from `user_profiles` after reads are fully migrated to `subscriptions`
- [ ] 2.6 Rework RLS policies so authenticated user-scoped access is meaningful again
- [ ] 2.7 Clean up legacy Stripe-only columns and event remnants while touching the schema

## 3. Backend: JWT-Only User-Facing Edge Functions

- [ ] 3.1 Add a shared auth helper for edge functions that verifies Supabase JWTs and resolves the current user
- [ ] 3.2 Update `get-subscription-status` to require Supabase JWT auth and derive plan state from `subscriptions`
- [ ] 3.3 Create or update `check-reminder-limit` to require Supabase JWT auth and return `{ allowed, limit, current_count }`
- [ ] 3.4 Update `create-paddle-checkout` to use the authenticated Supabase user ID instead of email or Google-token lookup
- [ ] 3.5 Update authenticated customer-portal/account endpoints to require Supabase JWT auth
- [ ] 3.6 Keep webhook/admin paths on service role only, but remove legacy custom-auth assumptions from shared code

## 4. Extension: Supabase Session Management

- [ ] 4.1 Replace `chrome.identity.getAuthToken()`-based auth entry points with a Supabase Google sign-in flow launched via `chrome.identity.launchWebAuthFlow(...)`
- [ ] 4.2 Implement auth callback handling that parses the final redirect URL and constructs the Supabase session locally
- [ ] 4.3 Persist the Supabase session (`access_token`, `refresh_token`, expiry metadata) in `chrome.storage.local`
- [ ] 4.4 Add centralized session restore, refresh, and sign-out logic in the service worker
- [ ] 4.5 Remove legacy token-decoding and Google-token cache assumptions from auth/session code
- [ ] 4.6 Remove obsolete `oauth2` manifest usage once the new flow is fully cut over

## 5. Extension: Subscription and Premium State

- [ ] 5.1 Update `account-service.js` to use the Supabase session for subscription refreshes
- [ ] 5.2 Update `storage-service.js` cache schema to track session-backed subscription state and refresh timestamps
- [ ] 5.3 Update popup initialization to restore session state first, then refresh subscription state in the background
- [ ] 5.4 Replace legacy auth-recovery behavior with session refresh or interactive Supabase sign-in
- [ ] 5.5 Preserve the popup account-state precedence model (verified premium > recovery/sign-in > upgrade prompt)

## 6. Extension: Backend Plan Enforcement

- [ ] 6.1 Call `check-reminder-limit` before reminder creation using the authenticated Supabase session
- [ ] 6.2 Update reminder creation flows to use conservative fallback behavior when backend verification is unavailable
- [ ] 6.3 Remove or refactor local-only premium enforcement helpers that can bypass backend entitlement checks

## 7. Payments and Subscription Lifecycle

- [ ] 7.1 Update Paddle checkout to send `auth.users.id` in `custom_data.user_id`
- [ ] 7.2 Update webhook handling so subscription records stay keyed to the authenticated user identity model
- [ ] 7.3 Ensure the portal / manage-subscription flow works with Supabase JWT auth instead of raw Google tokens

## 8. Dead Code and Legacy Path Removal

- [ ] 8.1 Remove custom Google-token validation paths that are no longer used after the Supabase Auth cutover
- [ ] 8.2 Remove stale `subscription-service.js` placeholders and dead message types that assumed the old auth model
- [ ] 8.3 Clean up service-worker and popup handlers tied to the old `chrome.identity.getAuthToken()` entitlement flow

## 9. Testing

- [ ] 9.1 Write Playwright tests for Supabase sign-in, callback handling, and session restore
- [ ] 9.2 Write Playwright tests for access-token expiry and refresh-token-based session recovery
- [ ] 9.3 Write Playwright tests for popup premium-state changes driven by JWT-backed subscription refreshes
- [ ] 9.4 Write Playwright tests for backend plan enforcement under authenticated, unauthenticated, and backend-unavailable states
- [ ] 9.5 Update checkout / customer-portal tests to reflect Supabase JWT auth and `auth.users.id`-based identity linkage
